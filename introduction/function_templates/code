#include <iostream>

template<typename T>
void swap(T &a, T &b) {
    T temp = a;  // Store a in temp
    a = b;        // Assign b to a
    b = temp;    // Assign temp (original a) to b
}

int main()
{
    char digit = '3';
    char letter = 'a';
    swap<char>(digit, letter);
}